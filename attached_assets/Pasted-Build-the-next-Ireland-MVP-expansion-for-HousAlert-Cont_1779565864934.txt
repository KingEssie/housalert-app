Build the next Ireland MVP expansion for HousAlert.

Context:
Daft.ie is now successfully working through ScraperAPI.
The Ireland scheduler is active.
Listings are already flowing into the existing ingestion pipeline.

Current priorities:
1. Improve first-user experience with recent Dublin listings
2. Prepare additional Ireland sources
3. Keep everything simple and stable

IMPORTANT:
Do NOT touch:
- service worker
- push/VAPID/FCM
- Stripe/paywall
- matching logic
- Germany infrastructure
- existing stable ingestion architecture

Do NOT over-engineer.

==================================================
1. RECENT LISTINGS / BACKFILL UX
==================================================

Inspect how new users currently receive initial listings.

Goal:
A new Dublin user should immediately see recent Dublin rentals already ingested in the last few days, even before new realtime listings arrive.

Requirements:
- recent listings visible in normal listing feed/search experience
- do NOT trigger push notifications for old listings
- old listings should still be matchable/viewable in-app
- preserve existing dedupe behavior

If needed:
Add a lightweight freshness distinction:
- realtime/new
- recently found

Do NOT create a complex ranking system.

==================================================
2. ADD RENT.IE MVP FETCHER
==================================================

Create a working Rent.ie Ireland source using the same pattern as Daft.

Requirements:
- proxy-compatible using existing DAFT_PROXY_URL or generic proxy support
- normalized SourceListing output
- defensive parsing
- stable external IDs
- absolute URLs
- numeric prices if possible
- Dublin-focused initially

Preferred structure:
server/sources/ireland/rentie/

Reuse the Ireland orchestrator and existing ingestion pipeline.

==================================================
3. ADD MYHOME.IE MVP FETCHER
==================================================

Same goal as Rent.ie.

Requirements:
- lightweight implementation
- defensive parser
- normalized listings
- existing ingestion pipeline
- proxy-compatible if needed

Do NOT add browser automation unless absolutely necessary.

==================================================
4. SCHEDULER INTEGRATION
==================================================

Wire Rent.ie and MyHome.ie into the existing Ireland scheduler.

Requirements:
- safe error handling
- no crash loops
- clear logs:
  - source
  - fetched
  - normalized
  - inserted
  - duplicates
  - matches
  - errors

Keep intervals conservative initially.

==================================================
5. SOURCE HEALTH
==================================================

Ensure:
- Rent.ie and MyHome.ie appear in source health/admin
- last success/error tracked
- listings counts tracked

Reuse existing monitoring systems.

==================================================
6. DO NOT CREATE FAKE DATA
==================================================

If a source fails or blocks:
- return []
- log clearly
- do not inject fake listings

==================================================
7. MINIMAL VERIFICATION
==================================================

Run only lightweight checks:
- server starts
- scheduler starts
- fetchers compile
- no SW/push changes
- no Germany changes

If live listings are successfully fetched:
show 1–3 sample normalized listings per source.

==================================================
8. REPORT BACK
==================================================

Report:
- files created/modified
- whether Rent.ie worked
- whether MyHome.ie worked
- whether proxies were required
- sample listing counts
- any anti-bot risks
- whether recent listing UX/backfill behavior was improved