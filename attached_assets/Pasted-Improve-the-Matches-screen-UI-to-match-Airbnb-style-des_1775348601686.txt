Improve the Matches screen UI to match Airbnb-style design and fix all layout and interaction issues.

-------------------------------------
1. MATCHES TABS (AIRBNB STYLE)
-------------------------------------

Replace current tabs with a segmented control (pill style):

- Container:
  - display: flex
  - background: #F3F4F6
  - border-radius: 999px
  - padding: 4px
  - width: fit-content

- Tabs: "Matches", "Gereageerd", "Favorieten"

- Active tab:
  - background: white
  - border-radius: 999px
  - padding: 8px 14px
  - font-weight: 600
  - color: black
  - subtle shadow

- Inactive tab:
  - background: transparent
  - color: #6B7280
  - font-weight: 500

- Remove underline completely


-------------------------------------
2. LISTING CARD IMAGE (AIRBNB STYLE)
-------------------------------------

Fix image proportions and styling:

- Use:
  aspect-ratio: 4 / 3
  OR fixed height: 240px

- Add:
  border-radius: 16px
  overflow: hidden

- Ensure images fully cover:
  object-fit: cover


-------------------------------------
3. FAVORITE HEART (FIX DESIGN)
-------------------------------------

Remove circular background completely.

- Position:
  top-right inside image

- Style (not favorited):
  - icon: outline heart
  - color: white
  - NO background container

- Add subtle shadow or gradient overlay behind icon for visibility

- Style (favorited):
  - icon: filled heart
  - color: #FF385C (Airbnb red/pink)
  - NO circle background

- Make clickable:
  toggle favorite state


-------------------------------------
4. TYPOGRAPHY + CONTENT STRUCTURE
-------------------------------------

Fix listing text layout EXACTLY like this:

[TITLE]
[ADDRESS]
[DETAILS]
[WEBSITE]

-------------------------------------

TITLE:
- font-size: 16px
- font-weight: 600 (NOT bold)
- max 1 line
- truncate with ellipsis

-------------------------------------

ADDRESS:
- format: "Street 12, München"
- font-size: 14px
- color: #6B7280

-------------------------------------

DETAILS ROW:
Show:
- 🛏 bedrooms
- 📐 square meters

Example:
"🛏 1 slaapkamer · 📐 45 m²"

- font-size: 14px
- color: #6B7280
- icons in dark gray (#374151)

-------------------------------------

WEBSITE:
- font-size: 13px
- color: #9CA3AF


-------------------------------------
5. DATA FIXES (IMPORTANT)
-------------------------------------

Update data mapping:

- Title = listing title (shortened)
- Address = street + house number + city
- Remove district-based formatting
- Ensure backend sends:
  street
  house_number
  city

Fallback if missing:
- show only city


-------------------------------------
6. ADD ICONS (DETAILS ROW)
-------------------------------------

Add icons:

- Bedroom → bed icon
- Size → square / ruler icon

- Size: 14px
- Color: #374151


-------------------------------------
7. CARD ACTIONS
-------------------------------------

Ensure these are visible:

- Heart icon (top-right of image)
- Click toggles favorite

Block action:
- NOT on card
- Only on detail page (clean UX)


-------------------------------------
8. SPACING + LAYOUT CLEANUP
-------------------------------------

- Add spacing between elements:
  gap: 4px–6px between text lines

- Card spacing:
  margin-bottom: 16px

- Keep layout minimal and clean


-------------------------------------
9. GENERAL RULE
-------------------------------------

Make the UI feel like Airbnb:
- clean
- light
- minimal
- no heavy borders
- no unnecessary containers

Avoid:
- thick outlines
- dark backgrounds
- oversized text


-------------------------------------
EXPECTED RESULT
-------------------------------------

Each listing should look like:

[IMAGE 4:3 rounded]
   ❤️

TITLE (1 line)
Street 12, München
🛏 1 slaapkamer · 📐 45 m²
immowelt.de

-------------------------------------

Do NOT change backend logic unless needed for address formatting.
Focus on UI and mapping only.