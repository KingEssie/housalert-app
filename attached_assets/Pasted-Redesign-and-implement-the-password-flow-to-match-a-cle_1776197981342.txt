Redesign and implement the password flow to match a clean, minimal, high-conversion UX similar to Rentbird.

IMPORTANT:
This is BOTH a UI and functional change.
Do NOT partially implement.
Do NOT approximate styling.
Follow EXACT behavior and structure.

==================================================
1. SIMPLIFY STRUCTURE (CRITICAL)
==================================================

Replace current password change screen:

REMOVE:
- "Huidig wachtwoord" field completely

KEEP ONLY:
1. "Wachtwoord"
2. "Wachtwoord bevestigen"

Apply this to:
- Change password screen
- Registration flow (same logic + same UI)

==================================================
2. MINIMAL CLEAN UI (RENTBIRD STYLE)
==================================================

Design rules:

- NO icons inside input fields (remove lock icons)
- Optional: keep eye icon for visibility toggle on the right
- Inputs must look like clean, flat fields (not bubbly)

INPUT STYLE:
- background: white
- border: 1px solid #D1D5DB
- border-radius: 8px
- height: ~56px
- padding-left/right: 16px+

TYPOGRAPHY:
- Labels: bold, dark (#111), slightly larger
- Input text: regular weight
- Placeholder: light grey

SPACING:
- Clean vertical spacing between fields
- No clutter
- No unnecessary UI elements

==================================================
3. PASSWORD VALIDATION (CORE FEATURE)
==================================================

Implement real-time validation.

PASSWORD RULES:
- Minimum 8 characters
- At least 1 uppercase letter
- At least 1 number

==================================================
4. LIVE VALIDATION UI (LIKE RENTBIRD)
==================================================

Under the password input, show validation feedback:

Example text:

"Je wachtwoord heeft minimaal  
8 karakters, 1 hoofdletter en 1 cijfer nodig"

Behavior:

- Each rule updates LIVE while typing
- Invalid parts = red (#E11D48)
- Valid parts = default or green

Example:
❌ 8 karakters (red if not met)
❌ 1 hoofdletter
❌ 1 cijfer

→ turns valid when conditions are met

NO animations needed, just instant feedback

==================================================
5. CONFIRM PASSWORD LOGIC
==================================================

- Second field: "Wachtwoord bevestigen"
- Must match first password

If mismatch:
- show inline error (red text)
- do NOT allow submission

==================================================
6. CTA BUTTON BEHAVIOR
==================================================

Button: "Opslaan" or "Wachtwoord wijzigen"

States:

DISABLED:
- if password invalid
- if confirm does not match

ENABLED:
- only when ALL conditions are met

STYLE:
- disabled → light pink / muted
- enabled → strong primary color (pink)
- full width button

==================================================
7. REGISTRATION FLOW (IMPORTANT)
==================================================

Apply EXACT SAME logic to:

- Signup / registration screen

Do NOT duplicate logic differently.

Create reusable validation logic:
- shared password validator
- shared UI component

==================================================
8. UX PRINCIPLES (NON-NEGOTIABLE)
==================================================

- Reduce friction (fewer fields)
- Give instant feedback
- Keep UI minimal and calm
- Make errors clear but not aggressive
- Guide user toward success

==================================================
9. OUTPUT (MANDATORY)
==================================================

Return:

1. Which components were created or updated
2. Where password validation logic is implemented
3. Exact validation rules enforced
4. Confirmation that:
   - live validation works
   - confirm password check works
   - button enables/disables correctly
5. Confirmation that registration uses the SAME logic