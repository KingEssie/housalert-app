Unify the entire password reset flow into one consistent HousAlert-branded experience.

Scope:
- Forgot password screen
- Reset password email template
- New password form screen
- Success/verification screens related to password reset
- Shared auth form styling if needed for consistency

Important:
- Keep all existing password reset functionality intact
- This is primarily a branding + consistency pass
- Reuse the current input heights, button heights, and corner radius from the forgot-password screen as the standard for this whole auth/reset flow

==================================================
1. FORGOT PASSWORD SCREEN
==================================================

Update the forgot-password screen with these exact changes:

- Add the HousAlert logo at the top-left of the inner card/content area
- Keep the current general composition and current successful styling
- Change "Terug naar inloggen" text color to white
- Keep the current input field height, button height, and corner radius exactly as they are now
- Treat those dimensions as the reference standard for the rest of this flow

==================================================
2. CREATE A SHARED AUTH/RESET STYLE BASE
==================================================

Take the currently approved styling from the forgot-password screen and use it as the visual baseline for:
- input field height
- input corner radius
- primary button height
- button corner radius
- spacing between labels, fields, and buttons

Apply that same sizing consistently to:
- forgot password screen
- new password screen
- success/verification screens in this password flow

Goal:
- the entire password reset journey should feel like one designed system

==================================================
3. RESET PASSWORD EMAIL TEMPLATE
==================================================

The password reset email currently is not in HousAlert branding.

Update the password reset email template so it uses HousAlert branding consistently.

Requirements:
- HousAlert logo / brand at the top
- HousAlert colors instead of default/unbranded styling
- clean email layout
- strong CTA button
- keep the actual reset link functionality intact
- preserve good email readability and compatibility

Use HousAlert brand direction:
- brand primary blue styling
- clean white email background
- strong, modern CTA
- remove any default/raw/unbranded Supabase look where possible

Important:
- if Supabase email template configuration is involved, wire it correctly to the branded template
- do not break the reset token / redirect behavior

==================================================
4. NEW PASSWORD SCREEN
==================================================

Target:
- the screen where the user enters a new password after clicking the email link

Current issue:
- this screen is not in HousAlert branding

Fix:
- make it visually match the forgot-password screen
- use the same background treatment
- use the same inner card/panel style
- use the same logo placement
- use the same input heights and corner radius
- use the same button proportions and style language

This screen should feel like the second step of the same flow, not like a different product.

==================================================
5. SUCCESS / VERIFICATION SCREEN AFTER PASSWORD CHANGE
==================================================

Current issue:
- the verification/success screen after changing the password is not visually aligned with the rest

Fix:
- make the success screen after password change use the same visual style as the success/verification screen shown after sending the password reset link
- same overall structure
- same typography style
- same spacing rhythm
- same button style
- same branded feeling

Goal:
- both verification/success states in this flow should feel like siblings in the same UI system

==================================================
6. VISUAL CONSISTENCY RULE
==================================================

Across the entire password-reset flow, ensure consistency in:
- logo placement
- background usage
- panel/card styling
- label styling
- input styling
- button styling
- success state styling
- spacing
- typography hierarchy

==================================================
7. DO NOT CHANGE
==================================================

- actual password reset logic
- reset token handling
- auth routing beyond what is needed for correct visual flow
- successful current behavior where the email is sent correctly

==================================================
8. OUTPUT
==================================================

Return:
1. files changed
2. which shared auth/reset styles were standardized
3. confirmation the HousAlert logo is now on the forgot-password screen
4. confirmation "Terug naar inloggen" is now white
5. confirmation the reset email template is now in HousAlert branding
6. confirmation the new password screen now matches the forgot-password screen visually
7. confirmation the success screen after password change now matches the reset-link verification screen style